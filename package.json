{
  "name": "mypeptideprice-clean-static-site",
  "private": true,
  "type": "module",
  "dependencies": {
    "@netlify/blobs": "^10.0.8"
  },
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "test:catalog": "node scripts/test-catalog.mjs",
    "validate": "node scripts/validate-build.mjs",
    "test": "npm run test:catalog && npm run validate",
    "build:fallback:seed": "node scripts/build-catalog-fallback.mjs",
    "snapshot:live": "node scripts/build-catalog-live.mjs"
  }
}
