ENTRY POPUP FIX

Updated file:
- index.html
- glp-weight-loss.html

Changes:
- Prevented the main Agree & Enter Site button from bypassing the four required acknowledgments.
- Disabled the enter button until every checkbox is selected.
- Kept the Accept All shortcut working as a single-click acknowledgment and entry option.
- Raised the full legal disclosure layer above the entry popup so the View full disclaimers button works correctly.
- Locked background scrolling while the entry popup is open and restored scrolling after entry.
- Added safe session storage handling so the popup remains stable if browser storage is restricted.
- Repaired corrupted word-boundary regular expressions in the homepage and GLP landing page product-normalization logic.
