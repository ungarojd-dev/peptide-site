ENTRY POPUP FORMATTING CLEANUP

Updated file:
- index.html

Changes:
- Replaced the entry popup's old inline styles with the responsive popup classes already defined in the homepage stylesheet.
- Reformatted the popup into a clean mobile-first layout with a fixed header, scrollable acknowledgment body, and structured action footer.
- Added clear checked-state styling to each acknowledgment card.
- Reordered mobile actions so the primary Agree & Enter Site button spans the full width above the secondary options.
- Kept the repaired acceptance logic, Accept All shortcut, full disclosure overlay, and background scroll lock intact.

Validation:
- Confirmed manual four-checkbox entry flow on a 390px mobile viewport.
- Confirmed View full legal disclosures opens and returns correctly on mobile.
- Confirmed Accept All entry flow on desktop.
- Confirmed no browser runtime errors during popup interactions.
- Confirmed all executable inline JavaScript blocks pass syntax validation.
